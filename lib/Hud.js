/**
 *
 * @param game
 * @constructor
 */

Stravi.Hud = function(game) {
    /*
    this.hud = new Kinetic.Stage({
        "container": "hudCanvas",
        "width": game.canvasWidth,
        "height": game.canvasHeight,
        "x": 0,
        "y": 0,
    });
    
    this.hudLayer = new Kinetic.Layer();
    this.hudHex = new Kinetic.RegularPolygon({
        sides: 6,
        radius: 150,
        stroke: 'red',
        fill: 'white',
        x: game.canvasWidth,
        y: game.canvasHeight
    });

    this.hudLayer.add(this.hudHex);
    this.hud.add(this.hudLayer);
    this.hud.draw();
    */
}